export enum DateFormat {
  ISO8601 = "YYYY-MM-DD",
  RFC3999 = "YYYY-MM-DDTHH:mm:ss.SSSZ",
}

export enum DATE {
  DAY = "day",
  MONTH = "month",
  YEAR = "year",
}

export enum NavigationAction {
  Previous = -1,
  Next = 1,
}
